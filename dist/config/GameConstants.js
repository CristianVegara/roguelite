/**
 * GameConstants — canvas dimensions only.
 *
 * Kept in a separate file so scene modules can import these constants
 * without creating a circular dependency with GameConfig (which imports
 * every scene).
 */
export const GAME_WIDTH = 480;
export const GAME_HEIGHT = 640;
